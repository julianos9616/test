{% extends 'base.html.twig' %}

{% block body %}
{{parent()}}



{# {{ dump(customers) }} #}

<div class="container">

    <br>
    <h3>Realizar compra!</h3>

    <br>
    
    <div class="form-control">
        {{form_start(form)}}
                    
        {{form_end(form)}}
    </div>
</div>
{% endblock %}