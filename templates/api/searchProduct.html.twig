{# this tag only applies to the forms defined in this template #}
{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% extends 'base.html.twig' %}

{% block body %}
{{parent()}}

<div class="container">
    <div class="mt-3">
        <h3>A continuación podrás buscar el producto y su información por código</h3>
        <br>
    </div>
    <div class="form-control">
        {{form_start(form)}}
                    
        {{form_end(form)}}
    </div>
</div>
    
{% endblock %}